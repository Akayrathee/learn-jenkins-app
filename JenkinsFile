pipeline {
    agent any 
        stages {
            stage('Docker NPM'){
                
                agent docker {
                    image 'node:18-alpine'
                }
                steps{
                    sh '''
                    node --version
                    npm --version
                    '''
                }    
            }
        }
}